example plugin in which you can define:
	node types
	agent types
	type handlers

In order to build, you need to specify to reference 'BehaviacDesignerBase.dll', 
which is in the folder of BehaviacDesigner.exe.

The generated dll needs to be put into the folder of BehaviacDesigner.exe.
